# 긴 글 생성 기능

**질문과 요구에 대한 상세한 글 자동 생성**

---

## 🎯 기능 개요

사용자가 질문이나 요구를 입력하면, 시스템이 자동으로 상세하고 포괄적인 긴 글을 생성합니다.

---

## ✨ 주요 특징

### 1. 자동 감지
- **긴 글 생성 키워드**: "글", "작성", "생성", "만들어", "에세이", "문서", "상세하게", "자세히", "길게", "포괄적으로" 등
- **질문 키워드**: "?", "질문", "궁금", "알려줘", "설명해줘", "알려주세요" 등

### 2. 상세 글 생성
- **구조화된 형식**: 서론, 본론, 결론
- **구체적 예시**: 실제 사례와 예시 포함
- **마크다운 형식**: 제목, 소제목, 목록, 강조 등
- **최소 길이**: 300-500자 이상

### 3. 최적화된 설정
- **토큰 제한**: 긴 글 생성 시 2000 토큰, 일반 모드 1000 토큰
- **시스템 프롬프트**: 모드에 따라 다른 지침 제공

---

## 📝 사용 예시

### 예시 1: 명시적 요청
```
사용자: "인공지능에 대해 글 작성해줘"

시스템: 자동으로 긴 글 생성 모드 활성화
- 서론: 인공지능 소개
- 본론: 역사, 기술, 응용 분야 등 상세 설명
- 결론: 미래 전망
```

### 예시 2: 질문 형태
```
사용자: "Python 프로그래밍이란 무엇인가요?"

시스템: 자동으로 상세 답변 모드 활성화
- 명확한 답변
- 배경 설명
- 구체적 예시
- 관련 정보
```

### 예시 3: 일반 대화
```
사용자: "안녕하세요"

시스템: 일반 모드로 간결한 인사 응답
```

---

## 🔧 기술 구현

### 백엔드 (`backend/llm_service.py`)

#### 1. 메시지 강화 (`_enhance_with_knowledge`)
```python
def _enhance_with_knowledge(self, message: str, context: Optional[Dict]) -> tuple[str, bool]:
    """지식 베이스를 활용하여 메시지 강화
    
    Returns:
        tuple: (enhanced_message, is_long_form)
    """
    # 키워드 감지 및 프롬프트 강화
    # is_long_form 플래그 반환
```

#### 2. 시스템 프롬프트 (`_get_system_prompt`)
```python
def _get_system_prompt(self, is_long_form: bool = False) -> str:
    """시스템 프롬프트 생성
    
    Args:
        is_long_form: 긴 글 생성 모드 여부
    """
    # 모드에 따라 다른 지침 제공
```

#### 3. LLM 호출 메서드
- `_call_openai`: OpenAI API 호출 (max_tokens 조정)
- `_call_anthropic`: Anthropic API 호출 (max_tokens 조정)
- `_call_ollama`: Ollama API 호출 (프롬프트 강화)
- `_call_notebook_llm`: 노트북 LLM 호출 (프롬프트 강화)

---

## 📊 작동 방식

```
사용자 입력
    ↓
키워드 감지
    ↓
긴 글 생성 모드? → Yes → 프롬프트 강화 + max_tokens 증가
    ↓ No
일반 모드 → 간결한 답변
```

---

## 🎨 프론트엔드 연동

프론트엔드(`ChatGPTInterface.tsx`)는 별도 수정 없이 자동으로 작동합니다:

1. 사용자가 메시지 입력
2. 백엔드로 전송 (`/api/chat`)
3. 백엔드에서 자동 감지 및 처리
4. 긴 글 응답 수신 및 마크다운 렌더링

---

## 🚀 사용 방법

### 기본 사용
1. ChatGPT 인터페이스에서 질문이나 요구 입력
2. 시스템이 자동으로 감지하여 긴 글 생성
3. 마크다운 형식으로 렌더링된 상세한 글 확인

### 예시 입력
- "인공지능의 역사에 대해 글 작성해줘"
- "React는 무엇인가요?"
- "기후변화에 대해 상세하게 설명해줘"
- "Python 프로그래밍 기초를 알려주세요"

---

## ⚙️ 설정

### 환경 변수
기존 LLM 설정을 그대로 사용:
- `LLM_PROVIDER`: openai, anthropic, ollama, notebook
- `OPENAI_API_KEY`: OpenAI API 키
- `ANTHROPIC_API_KEY`: Anthropic API 키
- `OLLAMA_BASE_URL`: Ollama 서버 URL

### 토큰 제한
- **긴 글 생성 모드**: 2000 토큰
- **일반 모드**: 1000 토큰

---

## 📈 성능

- **응답 시간**: 일반 모드 대비 약 1.5-2배 증가 (긴 글 생성)
- **토큰 사용량**: 일반 모드 대비 약 2배 증가
- **품질**: 구조화되고 상세한 내용 제공

---

## 🔍 키워드 목록

### 긴 글 생성 키워드
- 글, 작성, 생성, 만들어, 작성해줘, 생성해줘, 만들어줘
- 글쓰기, 에세이, 문서, 상세하게, 자세히, 길게
- 포괄적으로, 전체적으로

### 질문 키워드
- ?, 질문, 궁금, 알려줘, 설명해줘, 알려주세요

---

## ✅ 완료 상태

- ✅ 백엔드 자동 감지 로직 구현
- ✅ 시스템 프롬프트 개선
- ✅ LLM 호출 메서드 수정
- ✅ 토큰 제한 조정
- ✅ 프론트엔드 자동 연동

---

**이제 질문이나 요구를 입력하면 자동으로 상세한 긴 글이 생성됩니다!** 🎉

