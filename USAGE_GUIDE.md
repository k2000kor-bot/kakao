# CORBU.AI 사용 가이드 (메뉴얼)

**CORBU.AI 시스템의 화면 구성, 메뉴별 기능, 프로젝트·대화·고급 기능 사용법을 상세히 안내합니다.**

### 이 문서 읽는 방법

- **처음 사용**: [§2 시작하기](#2-시작하기)로 실행·접속 후 [§5.6 시나리오 A](#56-시나리오별-플로우)대로 첫 대화까지 진행. 화면이 익숙해지면 [§1 화면 구성](#1-화면-구성레이아웃)·[§4 프로젝트 관리](#4-프로젝트-관리-상세) 순으로 보면 됩니다.
- **이미 사용 중**: 하고 싶은 작업에 맞는 메뉴·버튼은 **목차 아래 "무엇을 할 때 어디를 누르나요"** 표에서 찾고, 상세 절차는 해당 섹션으로 이동.
- **문제 발생 시**: [§8 문제 해결](#8-문제-해결)·[§10 FAQ](#10-자주-묻는-질문faq)·[§7.2 자주 보이는 메시지와 의미](#72-자주-보이는-메시지와-의미)를 참고.
- **메뉴얼 한 페이지로만 보고 싶다면**: [메뉴얼 빠른 참조](./docs/guides/MANUAL_QUICK_REFERENCE.md)에서 화면 구조·핵심 3단계·버튼 위치·접속 주소·체크리스트를 확인하세요.

---

## 목차

1. [화면 구성(레이아웃)](#1-화면-구성레이아웃)
2. [시작하기](#2-시작하기)
3. [메뉴별 기능](#3-메뉴별-기능)
4. [프로젝트 관리 (상세)](#4-프로젝트-관리-상세)
5. [대화(대화) 사용법](#5-대화대화-사용법)
6. [고급 기능 (목소리 생성·노트북 LLM)](#6-고급-기능)
7. [키보드 단축키](#7-키보드-단축키)
8. [문제 해결](#8-문제-해결)
9. [용어 정리](#9-용어-정리)
10. [자주 묻는 질문(FAQ)](#10-자주-묻는-질문faq)
11. [참고 링크](#11-참고-링크)

---

### 처음 사용자 추천 경로

- **바로 써보고 싶다면**: [§2 시작하기](#2-시작하기) → [§5.6 시나리오 A (첫 대화까지)](#56-시나리오별-플로우)
- **화면이 어떻게 생겼는지 알고 싶다면**: [§1 화면 구성](#1-화면-구성레이아웃)
- **프로젝트·지침·파일을 쓰고 싶다면**: [§4 프로젝트 관리](#4-프로젝트-관리-상세) → [§5.6 시나리오 B·C](#56-시나리오별-플로우)
- **목소리 생성(TTS)·노트북 LLM을 쓰고 싶다면**: [§6 고급 기능](#6-고급-기능) → [§5.6 시나리오 D](#56-시나리오별-플로우)
- **문제가 생겼다면**: [§8 문제 해결](#8-문제-해결) 또는 [§10 FAQ](#10-자주-묻는-질문faq)
- **단축키만 보고 싶다면**: [§7 키보드 단축키](#7-키보드-단축키)

### 무엇을 할 때 어디를 누르나요

| 하고 싶은 것 | 누르는 곳 | 비고 |
|-------------|-----------|------|
| **새 프로젝트 만들기** | 사이드바 프로젝트 영역 **새 프로젝트** 버튼 | 모달에서 이름 입력 후 생성 |
| **프로젝트 바꾸기** | 사이드바 **프로젝트 목록**에서 원하는 프로젝트 클릭 | 선택된 항목이 강조됨 |
| **프로젝트 편집/삭제/공유** | 해당 프로젝트 오른쪽 **⋮(점 세 개)** → 편집/삭제/공유 | 편집 시 모달에서 필드 수정 후 저장 |
| **새 대화 시작** | **+ 새 대화** (대화 목록 위 또는 메인 상단) | 먼저 프로젝트 선택 필요 |
| **기존 대화 열기** | 사이드바 **대화 목록**에서 대화 클릭 | 해당 대화가 메인에 로드됨 |
| **메시지 보내기** | 입력창에 입력 후 **Enter** 또는 **전송** 버튼 | Shift+Enter는 줄바꿈 |
| **메시지에 파일 첨부** | 입력창에 파일 **드래그 앤 드롭** 또는 파일 선택 | 해당 메시지와 함께 전송 (프로젝트 파일과 구분) |
| **대화 삭제** | 해당 대화 항목의 **X** 또는 메뉴에서 **삭제** | 복구 불가 |
| **목소리 생성(TTS)** | 사이드바 **고급 기능** → 메인에서 **목소리 생성** 탭/카드 | URL 또는 프로젝트 보이스 선택 후 텍스트 입력 |
| **노트북 LLM 사용** | 사이드바 **노트북 LLM** | CORBU.AI에서 먼저 프로젝트 선택 |
| **전체 기능 맵 보기** | 사이드바 **전체 기능** | 카테고리별 기능 맵 확인 및 해당 기능으로 빠른 이동 |
| **모달 닫기** | **Escape** 또는 모달 바깥 클릭 | 저장하지 않고 닫기 |
| **사이드바 닫기(모바일)** | **Escape** 또는 배경 클릭 | 768px 미만일 때 |

---

## 1. 화면 구성(레이아웃)

### 1.1 전체 구조

- **좌측 사이드바**: 메뉴·프로젝트 목록·대화 목록
- **메인 영역**: 선택한 메뉴에 따른 대화/기능 화면
- **모바일(768px 미만)**: 햄버거 메뉴로 사이드바 열기/닫기 (Escape·배경 클릭으로 닫기)

### 1.2 사이드바 상단 메뉴

| 메뉴명 | 경로 | 설명 |
|--------|------|------|
| **CORBU.AI** | `/` | 메인 대화. 프로젝트별 대화·지침·파일 연동 |
| **간단 대화** | `/simple` | 간단한 대화 및 분석 |
| **고급 기능** | `/features` | 음성 인식·이미지 분석·예측·**목소리 생성(TTS)** |
| **전체 기능** | `/features-map` | 카테고리별 기능 맵 및 빠른 이동 |
| **노트북 LLM** | `/notebook` | 프로젝트별 LLM·보이스 소스·딥러닝 연동 |

### 1.3 CORBU.AI 화면 내 요소

| 요소 | 위치 | 동작 |
|------|------|------|
| **프로젝트 목록** | 사이드바 중단 | 클릭 시 해당 프로젝트의 대화만 표시, 선택된 항목은 강조 표시 |
| **새 프로젝트 버튼** | 프로젝트 영역 상단 | 클릭 시 프로젝트 생성 모달 열림 |
| **대화 목록** | 사이드바 하단 | 선택한 프로젝트의 대화 제목 목록. 클릭 시 해당 대화 로드. 보통 최신 대화가 위에 오거나 생성 순서로 표시됨 |
| **+ 새 대화** | 대화 목록 위 또는 메인 상단 | 클릭 시 현재 프로젝트에 새 대화 생성 |
| **메시지 입력창** | 메인 하단 | 텍스트 입력. Enter 전송, Shift+Enter 줄바꿈. 자동 높이 조절 |
| **전송 버튼** | 입력창 오른쪽 | 클릭 시 메시지 전송 (Enter와 동일) |
| **프로젝트 점 세 개(⋮)** | 각 프로젝트 항목 오른쪽 | 클릭 시 편집·삭제·공유 메뉴 표시 |
| **대화 메뉴** | 각 대화 항목(호버 시) | 해당 대화 삭제 등 |
| **토스트** | 화면 상단 또는 하단 | 저장 완료·복사 완료 등 짧은 안내 메시지. 잠시 후 자동으로 사라짐 |

### 1.4 모바일·접근성

- **768px 미만**: 상단 햄버거 아이콘 클릭 시 사이드바가 오버레이로 열림. **Escape** 키 또는 배경 클릭으로 닫기.
- **모바일에서 할 수 있는 것**: 프로젝트 선택·새 대화·메시지 전송·프로젝트 ⋮(편집·삭제·공유)·대화 삭제 등 데스크톱과 동일. 단, 사이드바는 **햄버거를 눌러 연 뒤** 메뉴·프로젝트·대화 목록을 사용하세요.
- **포커스**: 키보드로 메뉴·버튼·입력창 이동 가능. 포커스 시 아웃라인 표시.
- **스크롤**: 메인 영역·사이드바 각각 독립 스크롤. 새 메시지 시 자동으로 하단으로 스크롤.

### 1.5 지원 환경 (참고)

| 항목 | 권장 |
|------|------|
| **브라우저** | Chrome, Firefox, Safari, Edge 최신 버전. JavaScript 활성화 필요. |
| **화면 크기** | 데스크톱 1024px 이상 권장. 768px 미만에서는 모바일 레이아웃(햄버거 메뉴). |
| **로컬 저장** | 대화·프로젝트 정보 저장을 위해 로컬 스토리지 사용. 시크릿/비공개 모드에서는 제한될 수 있음. |

---

## 2. 시작하기

### 2.1 시스템 실행

```bash
# 1) 의존성 설치 (최초 1회)
cd backend && pip install -r requirements.txt
cd .. && npm install

# 2) 통합 실행 (권장)
./start_all.sh
```

- **종료**: 서버를 멈추려면 실행한 터미널에서 **Ctrl+C**.
- **개별 실행**(프론트·백엔드 따로 실행): [QUICK_START §2.2](./QUICK_START.md#22-방법-b-개별-실행) 참고.

**정상 기동 시 확인**: 브라우저에서 http://localhost:3000 접속 시 **CORBU.AI 로고/제목**, **사이드바(메뉴·프로젝트·대화 영역)**, **메인 영역**, **입력창**이 보이면 정상입니다. API 문서는 http://localhost:5002/api/docs 에서 확인할 수 있습니다.

### 2.2 접속 주소

| 용도 | 주소 |
|------|------|
| **프론트엔드(웹)** | http://localhost:3000 |
| **백엔드 API (대화·통합)** | http://localhost:5002 |
| **API 문서(Swagger)** | http://localhost:5002/api/docs |

**다른 기기에서 접속 시**: 같은 네트워크라면 `http://<실행 중인 PC의 IP>:3000`으로 접속할 수 있습니다. 방화벽에서 3000·5002 포트 허용 여부를 확인하세요.

### 2.3 LLM 설정 (선택)

- **노트북 LLM(로컬)**: `LLM_PROVIDER=notebook`, Ollama 실행 후 `ollama pull llama3.1:8b` 등
- **OpenAI**: `OPENAI_API_KEY`, `LLM_PROVIDER=openai`, `LLM_MODEL=gpt-3.5-turbo`
- **Anthropic**: `ANTHROPIC_API_KEY`, `LLM_PROVIDER=anthropic`, `LLM_MODEL=claude-3-5-sonnet-20241022`
- 미설정 시 기본(폴백) 모드로 동작

**다음 단계**: [§3 메뉴별 기능](#3-메뉴별-기능)에서 각 메뉴 역할을 확인한 뒤, [§4 프로젝트 관리](#4-프로젝트-관리-상세) 또는 [§5.6 시나리오 A](#56-시나리오별-플로우)로 첫 대화을 진행하세요.

---

## 3. 메뉴별 기능

**메뉴·URL 요약**: 사이드바에서 클릭하거나 아래 URL로 직접 접속할 수 있습니다.

| 메뉴 | URL | 한 줄 설명 |
|------|-----|------------|
| **CORBU.AI** | `/` | 메인 대화. 프로젝트별 대화·지침·파일 연동 |
| **간단 대화** | `/simple` | 프로젝트 없이 간단 대화·분석 |
| **고급 기능** | `/features` | 목소리 생성(TTS)·음성 인식·이미지 분석·예측 등 |
| **전체 기능** | `/features-map` | 카테고리별 기능 맵 및 빠른 이동 |
| **노트북 LLM** | `/notebook` | 프로젝트별 LLM·보이스 소스·딥러닝 (먼저 프로젝트 선택) |

**간단 대화 vs CORBU.AI**: 프로젝트 없이 바로 대화하려면 **간단 대화**(`/simple`)을 사용하세요. 프로젝트 단위로 대화·지침·파일을 관리하려면 **CORBU.AI**(`/`)에서 프로젝트를 선택한 뒤 대화하세요.

### 3.1 CORBU.AI (메인 대화)

- **역할**: 프로젝트 단위로 대화를 나누고, 프로젝트 지침·파일을 대화 맥락에 반영
- **사용 흐름**: 프로젝트 선택 → (선택) 지침/파일 설정 → 새 대화 또는 기존 대화 선택 → 메시지 입력·전송
- **자동 저장**: 대화·프로젝트 정보는 자동 저장

### 3.2 간단 대화

- **역할**: 별도 프로젝트 없이 간단한 대화·분석
- **경로**: 사이드바에서 "간단 대화" 클릭 또는 `/simple` 접속

### 3.3 고급 기능

- **역할**: 음성 인식·이미지 분석·예측·**목소리 생성(TTS)** 등
- **목소리 생성**: 고급 기능 패널 내 "목소리 생성" 탭에서 URL/프로젝트 보이스로 TTS 사용
- **경로**: "고급 기능" 클릭 또는 `/features` 접속

### 3.4 전체 기능

- **역할**: 카테고리별 기능 맵 확인 및 해당 기능으로 빠른 이동
- **경로**: "전체 기능" 클릭 또는 `/features-map` 접속
- **사용 방법**: 전체 기능 화면에서 카테고리별로 정리된 기능 목록을 보고, 원하는 기능(예: 목소리 생성·음성 인식·이미지 분석)을 클릭하면 해당 기능 화면으로 이동합니다.

### 3.5 노트북 LLM

- **역할**: 프로젝트별 노트북 LLM·보이스 소스·딥러닝 연동 (프로젝트 단위 개별 동작)
- **사용**: 프로젝트 선택 후 노트북 LLM 화면에서 해당 프로젝트 컨텍스트로 생성/스트리밍
- **경로**: "노트북 LLM" 클릭 또는 `/notebook` 접속

---

## 4. 프로젝트 관리 (상세)

**요약**: 새 프로젝트는 **새 프로젝트** 버튼 → 이름 입력 → **생성**. 선택은 목록에서 클릭. 편집/삭제/공유는 프로젝트 **⋮** → 해당 메뉴. 편집 시 이름·설명·태그·지침·가이드라인·파일 수정 후 **저장**.

### 4.1 프로젝트 생성

1. 사이드바에서 **"새 프로젝트"** (또는 프로젝트 영역의 생성 버튼) 클릭 → **프로젝트 생성 모달**이 열림
2. **프로젝트 이름** 입력 (필수, 1자 이상 권장). 이름이 비어 있으면 생성 버튼이 비활성화되거나 오류가 날 수 있음
3. **"생성"** 또는 **"프로젝트 만들기"** 클릭
4. 모달이 닫히고, 생성된 프로젝트가 **사이드바 프로젝트 목록**에 바로 표시됨

**참고**: 이름만 입력해도 생성 가능. 설명·태그·지침·파일은 생성 후 **⋮ → 편집**에서 추가 가능.

**실패 시**: 이름이 비어 있지 않은지 확인. 버튼이 비활성화되어 있으면 1자 이상 입력. 계속 생성되지 않으면 [§8.1 프로젝트가 생성되지 않음](#81-프로젝트가-생성되지-않음) 참고.

### 4.2 프로젝트 선택

- 사이드바의 **프로젝트 목록**에서 원하는 프로젝트 클릭
- 선택된 프로젝트의 대화만 표시되며, 새 대화도 해당 프로젝트에 연결됨

### 4.3 프로젝트 편집 (점 세 개 메뉴)

1. 프로젝트 항목 오른쪽 **⋮(점 세 개)** 클릭 → **편집** / **삭제** / **공유** 메뉴가 나타남
2. **편집** 선택 → **프로젝트 편집 모달**이 열림
3. **이름·설명·태그·지침·가이드라인·프로젝트 파일** 필드를 수정
4. 변경한 내용이 모두 원하는 대로인지 확인한 뒤 **저장** 클릭. 서버에 반영되면 **"설정이 저장되었습니다"** 토스트가 뜨고 모달이 닫힘

**저장 전 확인**: **취소** 버튼, **Escape** 키, 모달 바깥 클릭으로 닫으면 변경 사항이 저장되지 않습니다. 반드시 **저장**을 눌러야 서버에 반영됩니다.

**프로젝트 이름 변경**: 편집 모달에서 **이름** 필드만 수정한 뒤 **저장**하면 사이드바에 새 이름이 반영됩니다.

**지침(instructions)**  
- 프로젝트 내 모든 대화에 공통으로 적용되는 지시문. 예: "항상 한국어로 답변", "보고서 톤 유지"  
- 지침은 대화 시 백엔드와 연동되어 응답에 반영됨  
- 여러 줄 입력 가능. 노트북 LLM·딥러닝 연동 시 해당 프로젝트 지침이 프롬프트 보강에 사용됨

**프로젝트 파일**  
- **파일 추가** 버튼 클릭 후 문서·이미지·코드 등 선택 (여러 파일 동시 선택 가능)  
- 지원 형식 예: PDF, DOC/DOCX, TXT, MD, PNG, JPG, JS, TS, TSX, JSON 등  
- 업로드 중에는 버튼에 **"업로드 중..."** 표시, 완료 후 목록에 파일명·크기·타입 표시  
- **삭제**: 파일 항목의 휴지통 버튼으로 제거  
- 프로젝트 컨텍스트(projectKnowledge)로 활용되어 해당 프로젝트 대화 시 자동 참고됨

### 4.4 프로젝트 삭제

1. 프로젝트 **⋮** → **삭제** 선택
2. 확인 후 삭제 (해당 프로젝트의 대화 포함 삭제, 복구 불가)

**주의:** 삭제 전 필요한 대화·지침 내용은 미리 복사하거나 내보내기 해 두세요. 삭제 후에는 복구할 수 없습니다.

### 4.5 프로젝트 공유

1. 프로젝트 **⋮** → **공유** 선택
2. **공유 모달**이 열리면 링크 생성·권한(읽기/쓰기 등) 설정
3. 생성된 링크 또는 초대 방식을 통해 다른 사용자와 프로젝트 공유

**참고**: 공유 모달의 필드·옵션은 앱 버전에 따라 다를 수 있습니다. 링크가 노출되면 해당 링크를 가진 사용자가 프로젝트에 접근할 수 있으므로, 필요 시 권한·만료 설정을 확인하세요.

### 4.6 프로젝트 편집 모달 필드 상세

편집 모달(**⋮ → 편집**)에서 다음 필드를 수정할 수 있습니다.

| 필드 | 설명 | 비고 | 입력 예시 |
|------|------|------|----------|
| **이름** | 프로젝트 표시 이름 | 필수. 사이드바 목록에 표시됨 | 예: `마케팅 기획`, `테스트` |
| **설명** | 프로젝트에 대한 짧은 설명 | 선택. 프로젝트 상세·공유 시 활용 | 예: `2025년 캠페인용` |
| **태그** | 검색·분류용 태그 목록 | 선택. 쉼표 또는 엔터로 여러 개 입력 | 예: `마케팅`, `캠페인` |
| **지침(instructions)** | 프로젝트 내 모든 대화에 적용되는 공통 지시문 | 여러 줄 입력 가능. 노트북 LLM·딥러닝에도 반영 | 예: `항상 한국어로 답변하고, 존댓말을 사용해 주세요.` |
| **가이드라인(initialGuidelines)** | 노트북 LLM 등에서 참고하는 가이드라인 목록 | 목록 형태. 항목 추가/삭제 가능 | 예: `형식: 제목-본문-결론` |
| **파일** | 프로젝트에 첨부한 문서·이미지 등 | "파일 추가"로 업로드, 목록에서 개별 삭제 가능 | PDF, DOC, TXT, MD, PNG, JPG 등 (§4.7 참고) |

- 위 **입력 예시**는 참고용이며, 실제 값은 자유롭게 입력하면 됩니다.
- **필드 비우기**: 선택 필드(설명·태그·지침·가이드라인)는 비워 두어도 저장 가능합니다. **이름**만 비우면 저장이 되지 않거나 오류가 날 수 있습니다.

- **저장** 버튼 클릭 시 서버에 반영. 저장 성공 시 **토스트**로 "설정이 저장되었습니다" 안내가 뜨면 정상 반영된 것입니다.
- **취소** 또는 모달 바깥 클릭·**Escape** 시 변경 사항은 저장되지 않음.
- **주의**: 삭제(⋮ → 삭제)는 프로젝트와 해당 대화를 모두 제거하며 **복구할 수 없음**.

### 4.7 입력·저장 제한 (참고)

| 항목 | 제한 또는 비고 |
|------|----------------|
| **프로젝트 이름** | 필수. 빈 값이면 생성·저장이 되지 않을 수 있음 |
| **메시지 입력** | 한 번에 보낼 수 있는 텍스트 길이에 제한이 있을 수 있음 (예: 1만 자 단위). 너무 길면 여러 번 나눠 보내기 |
| **로컬 저장** | 대화·프로젝트 정보는 브라우저 로컬 스토리지 등에 저장됨. 일반적으로 약 5MB 수준 제한이 있음 |
| **프로젝트 파일** | 지원 형식·최대 용량은 백엔드 설정에 따름. 업로드 실패 시 파일 크기·형식 확인 |

- 위 수치는 버전에 따라 다를 수 있음. 오류 메시지가 나오면 해당 항목을 줄이거나 형식을 맞추면 됨.

**다음 단계**: [§5 대화(대화) 사용법](#5-대화대화-사용법)에서 새 대화·전송·기존 대화 이어하기를 진행하세요.

---

## 5. 대화(대화) 사용법

**요약**: **프로젝트 선택** → **+ 새 대화** → 입력창에 입력 → **Enter** 또는 **전송**. 기존 대화는 사이드바 대화 목록에서 클릭. 줄바꿈은 **Shift+Enter**. 삭제는 대화 항목 **X** 또는 메뉴에서 **삭제**.

### 5.1 새 대화 시작

1. **프로젝트 선택** (필수) — 사이드바 프로젝트 목록에서 클릭해 선택된 상태로 둠
2. **"+ 새 대화"** 클릭 — 대화 목록 위나 메인 상단에 있는 버튼
3. 메시지 입력 후 **Enter** 또는 **전송** 버튼으로 전송
4. 전송 후 입력창이 비워지고, AI 응답이 오면 메시지 영역에 순서대로 표시됨

**전송 전 확인**: 입력창이 비어 있으면 전송 버튼이 비활성화되어 있을 수 있습니다. 1자 이상 입력한 뒤 **Enter** 또는 **전송**을 누르세요.

**팁**: 프로젝트를 선택하지 않으면 "새 대화"가 어떤 프로젝트에 붙을지 정해지지 않을 수 있음. 반드시 먼저 프로젝트를 선택하세요.

### 5.2 기존 대화 이어하기

- 사이드바 **대화 목록**에서 원하는 대화 클릭 → 해당 대화 로드 후 이어서 입력

### 5.3 메시지 입력 규칙

| 동작 | 입력 |
|------|------|
| **전송** | **Enter** |
| **줄바꿈** | **Shift + Enter** |
| **긴 글 생성** | "글 작성해줘", "상세히 설명해줘", "에세이 써줘" 등 요청 시 상세 긴 글 자동 생성 |

- 입력창은 내용에 따라 **높이가 자동 조절**됨. 긴 메시지도 여러 줄로 입력 가능.
- **전송 버튼**: 입력창 오른쪽에 있으며, 클릭 시 현재 입력 내용이 한 번에 전송됨. 입력이 비어 있으면 비활성화되어 있을 수 있음.
- **메시지 길이**: 한 번에 보낼 수 있는 텍스트에 제한이 있을 수 있음. 너무 길면 오류가 나거나 잘릴 수 있으므로 [§4.7 입력·저장 제한](#47-입력저장-제한-참고) 참고.

### 5.4 메시지 기능

- **마크다운**: 코드 블록(언어 지정), 링크, 표, 리스트 등 지원. AI 응답에서도 마크다운 렌더링.
- **메시지 복사**: 각 메시지 영역의 **복사** 버튼 클릭 시 클립보드에 복사, 토스트로 "복사되었습니다" 안내.
- **자동 스크롤**: 새 메시지가 추가되면 메인 영역이 하단으로 스크롤.
- **스트리밍**: 응답이 스트리밍으로 올 경우 실시간으로 텍스트가 채워짐. (설정에 따라 비스트리밍도 가능)
- **응답 중**: AI 응답이 오는 중(스트리밍)에는 보통 다음 메시지 전송을 기다리거나, 앱에 따라 전송 버튼이 비활성화될 수 있음. 응답이 끝난 뒤 입력하면 됨.
- **대화 제목**: 첫 메시지 전송 후 자동으로 대화 제목이 생성되어 사이드바 목록에 표시됨.

### 5.5 대화 삭제

- 대화 항목의 **X** 버튼 또는 해당 대화의 메뉴(⋮)에서 **삭제** 선택  
- 삭제 시 해당 대화만 제거되며, 프로젝트와 다른 대화는 유지됨.

**주의:** 삭제된 대화는 복구할 수 없습니다. 필요한 메시지는 미리 복사해 두세요.

### 5.6 시나리오별 플로우

**시나리오 A: 첫 대화까지 (최소 단계)** — *예상 소요: 약 1~2분*

1. 브라우저에서 **http://localhost:3000** 접속
2. **새 프로젝트** 클릭 → 모달에서 이름 입력(예: `테스트`) → **생성** 클릭 → 모달 닫힘, 목록에 프로젝트 표시
3. 사이드바 **프로젝트 목록**에서 방금 만든 프로젝트 **클릭** (선택된 항목이 강조됨)
4. **+ 새 대화** 클릭
5. 하단 **입력창**에 메시지 입력(예: `안녕`) → **Enter** 또는 **전송** 버튼
6. 메시지가 전송되고, AI 응답이 오면 화면에 표시됨 (LLM 설정된 경우). 대화 제목이 자동 생성되어 사이드바 대화 목록에 보임

**예상 결과**: 입력창이 비워지고, 사용자 메시지가 메인에 표시된 뒤 AI 응답이 스트리밍 또는 한 번에 표시됨. 사이드바 대화 목록에 자동 생성된 제목이 나타남.

*실패했을 때*: 프로젝트가 목록에 안 보이면 새로고침(F5). AI 응답이 안 오면 [§8.2 LLM이 응답하지 않음](#82-llm이-응답하지-않음) 참고.

**시나리오 B: 프로젝트 지침을 적용한 대화** — *예상 소요: 약 2분*

1. **CORBU.AI** 메뉴에서 프로젝트 선택
2. 해당 프로젝트 **⋮** → **편집** 클릭
3. **지침** 필드에 입력(예: `항상 한국어로 답변하고, 존댓말을 사용해 주세요.`) → **저장**
4. **+ 새 대화** 또는 기존 대화 선택
5. 질문 입력 후 전송 → 응답이 지침에 맞게 나오는지 확인

**예상 결과**: "설정이 저장되었습니다" 토스트 후 모달 닫힘. 새/기존 대화에서 보낸 질문에 대한 응답이 지침(예: 한국어·존댓말)에 맞게 나옴.

*실패했을 때*: "설정이 저장되었습니다" 토스트가 안 뜨면 [§8.3 대화/프로젝트가 저장되지 않음](#83-대화프로젝트가-저장되지-않음) 참고. 응답에 지침이 반영 안 되면 저장 후 대화를 새로 열거나 새 대화에서 다시 질문해 보세요.

**시나리오 C: 프로젝트 파일을 참고한 대화** — *예상 소요: 약 3분 (업로드 시간 포함)*

1. **CORBU.AI**에서 프로젝트 선택 → **⋮** → **편집**
2. **파일 추가** 클릭 → 문서 또는 이미지 선택(여러 개 가능) → 업로드 완료될 때까지 대기
3. **저장** 클릭
4. **+ 새 대화** 또는 기존 대화에서 질문(예: `첨부한 파일 내용을 요약해 줘`) 입력 후 전송
5. 응답에 프로젝트 파일 맥락이 반영되는지 확인

**예상 결과**: "파일 추가" 버튼이 "업로드 중..." 후 목록에 파일명·크기 표시. 저장 토스트 후, 대화에서 질문 시 첨부 파일 내용이 반영된 요약·설명이 응답에 포함됨.

*실패했을 때*: 업로드가 끝나지 않으면 "업로드 중..." 상태에서 대기. 실패 시 파일 크기·형식 확인, [§10 FAQ – 파일 업로드](#10-자주-묻는-질문faq) 참고. 응답에 파일 내용이 안 나오면 저장 후 새 대화에서 질문해 보세요.

**시나리오 D: 목소리 생성(TTS) 한 번 해보기** — *예상 소요: 약 2~3분*

1. **고급 기능** 메뉴 클릭
2. **목소리 생성** 탭(또는 해당 카드) 선택
3. **URL 기반**: 보이스 소스 URL 입력 → (필요 시) 학습 완료 후 텍스트 입력 → **생성**/**재생**
4. **프로젝트 보이스**: 먼저 CORBU.AI에서 프로젝트에 보이스 소스(URL) 등록 후, 여기서 "프로젝트 보이스" 선택 → 텍스트 입력 → **생성**/**재생**

**예상 결과**: 입력한 텍스트가 선택한 보이스로 음성 생성·재생됨. (URL 기반 시 학습 완료 후, 프로젝트 보이스 시 해당 프로젝트 보이스로 재생.)

*실패했을 때*: 재생이 안 되면 [§8.5 목소리 생성(TTS) 오류](#85-목소리-생성tts-오류) 참고. URL·프로젝트 보이스 등록 여부, F12 Network에서 `/api/tts/` 요청 확인.

**다음 단계**: [§6 고급 기능](#6-고급-기능)에서 목소리 생성(TTS)·노트북 LLM 사용법을 확인하세요.

---

## 6. 고급 기능

**요약**: **고급 기능** 메뉴 → **목소리 생성** 탭에서 URL 또는 **프로젝트 보이스** 선택 후 텍스트 입력 → 생성/재생. **노트북 LLM**은 CORBU.AI에서 **프로젝트 선택** 후 진입하면 해당 프로젝트 지침·파일이 반영됨.

### 6.1 목소리 생성(TTS)

**진입**: 사이드바 **고급 기능** 클릭 → 메인 영역에서 **목소리 생성** 탭 선택.

**방법 1 – URL 기반**

1. **보이스 소스 URL** 입력 (YouTube/TikTok 등 영상 URL)
2. (필요 시) 시스템이 해당 영상에서 목소리를 학습
3. **재생할 텍스트** 입력
4. **생성** 또는 **재생** 버튼 클릭 → 해당 목소리로 TTS 재생

**방법 2 – 프로젝트 보이스**

1. **CORBU.AI** 메뉴에서 프로젝트 선택 후 **⋮ → 편집** 또는 노트북 LLM에서 해당 프로젝트의 **보이스 소스** 등록
2. **고급 기능** → **목소리 생성** 탭에서 **프로젝트 보이스** 선택
3. 프로젝트에 등록된 보이스로 텍스트 입력 후 생성·재생

**상황/스타일**: 톤·상황(예: 안내, 뉴스) 선택 시 스타일에 맞게 생성됨.

**재생이 안 되거나 오류가 나면**: [§8.5 목소리 생성(TTS) 오류](#85-목소리-생성tts-오류) 참고. URL·프로젝트 보이스 등록 여부, F12 Network에서 `/api/tts/` 요청 확인.

### 6.2 노트북 LLM (프로젝트별)

**프로젝트별 개별 동작**

1. **CORBU.AI** 메뉴에서 먼저 **프로젝트를 선택**한 뒤, 사이드바에서 **노트북 LLM** 클릭.
2. 노트북 LLM 화면에서는 **현재 선택된 프로젝트**의 지침·파일(projectKnowledge)이 자동으로 맥락에 반영됨.
3. **생성** 또는 **스트리밍** 요청 시 해당 프로젝트 지침이 백엔드·딥러닝 보강에 사용됨.

**보이스 소스(프로젝트별)**

- 노트북 LLM 화면 또는 프로젝트 설정에서 **보이스 소스** 탭/섹션 사용.
- **등록**: YouTube/TikTok 등 URL 입력 후 저장.
- **목록**: 해당 프로젝트에 등록된 보이스 소스 목록 확인.
- **삭제**: 항목별 삭제 가능. 삭제 후에는 해당 보이스로 TTS 불가.
- 등록된 프로젝트 보이스는 **고급 기능 → 목소리 생성**에서 "프로젝트 보이스"로 선택 가능.

**딥러닝 연동**

- 프로젝트 **지침(instructions)**이 있으면, 노트북 LLM 생성 시 딥러닝 분석·프롬프트 보강에 반영됨.
- 복잡도·주제·감정 분석과 함께 "프로젝트 지침에 맞춰 톤·형식·내용을 맞춰 답변" 등이 추가됨.

**프로젝트가 반영되지 않으면**: CORBU.AI에서 **먼저 프로젝트를 선택**했는지 확인. [§8.6 노트북 LLM이 프로젝트별로 동작하지 않음](#86-노트북-llm이-프로젝트별로-동작하지-않음) 참고.

### 6.3 고급 기능 탭 구성

**고급 기능** 메뉴(`/features`) 진입 후, 메인 영역에서 다음 탭/카드를 사용할 수 있습니다.

| 탭/기능 | 설명 | 사용 순서 |
|---------|------|------------|
| **목소리 생성(TTS)** | URL 또는 프로젝트 보이스로 텍스트를 음성으로 재생 | URL 입력 또는 프로젝트 보이스 선택 → 텍스트 입력 → 생성/재생 |
| **음성 인식** | 음성을 텍스트로 변환 | (해당 탭 안내에 따름) |
| **이미지 분석** | 이미지 업로드 후 분석·설명 | 이미지 선택/드래그 → 분석 요청 |
| **예측** | 데이터 기반 예측·트렌드 | (해당 탭 안내에 따름) |
| **기타** | 기능 맵에서 전체 기능 목록 확인 | **전체 기능** 메뉴(`/features-map`)에서 카테고리별 이동 |

- 탭이 없고 카드 형태일 수 있음. 화면에 표시되는 **목소리 생성**, **음성**, **이미지**, **예측** 등 레이블을 기준으로 선택하면 됨.
- **탭/카드 전환**: 메인 영역에서 **목소리 생성**, **음성 인식**, **이미지 분석**, **예측** 등 탭을 클릭하거나 카드를 클릭하면 해당 기능 화면으로 전환됩니다. 화면에 보이는 레이블을 클릭해 이동하세요.

### 6.4 기타 고급 기능

- 음성 인식·이미지 분석·예측 등은 **고급 기능** 화면 내 해당 탭/카드에서 사용. 각 기능 진입 후 입력 필드·버튼 안내에 따라 진행.

---

## 7. 키보드 단축키

| 동작 | 단축키 |
|------|--------|
| 메시지 전송 | **Enter** |
| 줄바꿈 | **Shift + Enter** |
| 포커스 이동 | **Tab** / **Shift+Tab** (메뉴·버튼·입력창 등) |
| 모달 닫기 | **Escape** |
| 사이드바 닫기(모바일) | **Escape** |
| 배경 클릭(모바일) | 사이드바 닫힘 |
| 입력 히스토리(이번 세션에서 보낸 메시지) | **↑** / **↓** (입력창 포커스 시) |

- **↑/↓**는 **입력창에 포커스가 있을 때만** 이번 세션에서 보낸 메시지 히스토리로 이동합니다. 포커스가 다른 곳에 있으면 동작하지 않을 수 있습니다.
- 단축키 도움말이 앱 내에 있는 경우, 해당 메뉴에서 **⌘F** 또는 **단축키** 안내를 확인하세요.

### 7.1 확인 포인트 (정상 동작 시 보이는 것)

| 동작 | 이렇게 보이면 정상 |
|------|-------------------|
| **프로젝트 생성** | 모달이 닫히고, 사이드바 프로젝트 목록에 새 이름이 바로 추가됨 |
| **프로젝트 선택** | 해당 프로젝트 항목이 강조(배경/테두리 등)되고, 대화 목록이 그 프로젝트 것으로 바뀜 |
| **프로젝트 편집 저장** | 토스트 **"설정이 저장되었습니다"** 표시 후 모달 닫힘 |
| **파일 업로드 중** | **"파일 추가"** 버튼이 **"업로드 중..."** 으로 바뀜 → 완료 후 목록에 파일명·크기 표시 |
| **메시지 전송** | 입력창이 비워지고, 사용자 메시지가 메인에 추가됨 → 이어서 AI 응답이 표시됨(LLM 설정 시) |
| **대화 생성** | 첫 메시지 전송 후 사이드바 대화 목록에 새 제목(자동 생성)이 나타남 |
| **메시지 복사** | 토스트 **"복사되었습니다"** (또는 유사 문구) 표시 |
| **목소리 생성(TTS) 재생** | 입력한 텍스트가 선택한 보이스로 음성 재생됨. 오디오 플레이어 또는 자동 재생 |
| **노트북 LLM 진입** | CORBU.AI에서 프로젝트 선택 후 노트북 LLM 클릭 시, 해당 프로젝트 지침·파일이 반영된 화면이 로드됨 |

### 7.2 자주 보이는 메시지와 의미

| 화면에 보이는 메시지 | 의미 | 조치 |
|---------------------|------|------|
| **설정이 저장되었습니다** | 프로젝트 편집이 서버에 반영됨 | 별도 조치 없음 |
| **복사되었습니다** | 메시지가 클립보드에 복사됨 | 붙여넣기로 사용 가능 |
| **프로젝트를 불러올 수 없습니다** | 해당 프로젝트 로드 실패(권한·삭제·네트워크 등) | F12 Network 확인, 프로젝트 재선택 또는 새로고침 |
| **업로드 중...** | 파일 업로드 진행 중 | 완료될 때까지 대기. 오래 걸리면 파일 크기·네트워크 확인 |
| **API 오류**, **네트워크 오류** 등 | 백엔드/네트워크 문제 | §8 문제 해결 참고, 백엔드 실행·포트 확인 |
| **토스트가 잠시 후 사라짐** | 저장·복사 안내 등 토스트는 자동으로 사라지는 정상 동작 | 별도 조치 없음 |

---

## 8. 문제 해결

### 8.1 프로젝트가 생성되지 않음

- 프로젝트 이름이 비어 있지 않은지 확인
- 브라우저 콘솔(F12) 및 네트워크 탭에서 API 오류 확인
- 로컬 스토리지 사용 가능 여부 확인 (F12 → Application → Local Storage)

### 8.2 LLM이 응답하지 않음

- 환경 변수 확인: `echo $LLM_PROVIDER`, `echo $OPENAI_API_KEY` 등
- Ollama 사용 시: `curl http://localhost:11434/api/tags` 또는 `ollama list`로 서버·설치된 모델 확인
- 백엔드 로그 확인 (터미널 출력 또는 로그 파일)

### 8.3 대화/프로젝트가 저장되지 않음

- 브라우저 로컬 스토리지 권한·용량 확인
- F12 → Console 에러 확인
- 캐시 삭제 후 재시도

### 8.4 화면이 로드되지 않음

- **강력 새로고침**: **Ctrl+F5**(Windows) 또는 **Cmd+Shift+R**(Mac)으로 캐시를 무시하고 새로고침
- F12 → Network 탭에서 실패한 요청 확인
- 프론트/백엔드 포트(3000, 5002) 사용 여부 확인

### 8.5 목소리 생성(TTS) 오류

- 백엔드 TTS API 상태 확인 (예: `/api/tts/` 관련 엔드포인트)
- 프로젝트 보이스 소스 등록 여부·URL 유효성 확인
- 브라우저 콘솔·네트워크 탭에서 오류 메시지 확인

### 8.6 노트북 LLM이 프로젝트별로 동작하지 않음

- **CORBU.AI** 메뉴에서 **먼저 프로젝트를 선택**한 뒤 **노트북 LLM** 메뉴로 이동했는지 확인
- 해당 프로젝트의 **지침·파일**이 편집 모달에서 저장되었는지 확인
- 백엔드 API(예: `GET /api/projects/{id}`)로 지침·파일이 반영되는지 확인
- 브라우저 콘솔·네트워크 탭에서 `notebook-llm` 관련 요청 오류 여부 확인

### 8.7 포트가 이미 사용 중일 때

- **프론트(3000)** / **백엔드(5002)** 충돌 시: `PORT=3001 npm start`, 백엔드는 해당 설정 파일에서 포트 변경
- 다른 프로세스 사용 여부: `lsof -i :3000`, `lsof -i :5002` (macOS/Linux)로 확인 후 필요 시 프로세스 종료 또는 포트 변경

**그래도 해결되지 않으면**: F12 Console·Network 탭의 오류 메시지, 수행하려던 작업, 브라우저·OS 정보를 정리해 [§10 FAQ](#10-자주-묻는-질문faq)를 다시 확인하거나, 저장소 이슈/지원 채널에 문의하세요.

---

## 9. 용어 정리

| 용어 | 설명 |
|------|------|
| **프로젝트** | 대화를 묶는 단위. 이름·설명·태그·지침·파일을 가짐. |
| **대화** | 하나의 대화 스레드. 메시지 목록과 제목을 가짐. 프로젝트에 속함. |
| **지침(instructions)** | 프로젝트에 설정하는 공통 지시문. 해당 프로젝트 모든 대화에 적용됨. |
| **가이드라인(initialGuidelines)** | 노트북 LLM 등에서 참고하는 가이드라인 목록. 프로젝트 편집 모달에서 목록 형태로 설정. 지침(한 덩어리 텍스트)과 구분됨. |
| **프로젝트 파일** | 프로젝트에 업로드한 파일. 대화 시 projectKnowledge로 참고됨. |
| **보이스 소스** | TTS용 목소리 원본. URL(YouTube/TikTok 등) 또는 프로젝트에 등록된 보이스. |
| **노트북 LLM** | 프로젝트별 맥락(지침·파일)을 반영한 LLM 생성·스트리밍 기능. |
| **고급 기능** | 목소리 생성(TTS), 음성 인식, 이미지 분석, 예측 등이 있는 메뉴. |

---

## 10. 자주 묻는 질문(FAQ)

**Q. 프로젝트 없이 대화할 수 있나요?**  
A. 메인 대화(CORBU.AI)에서는 **프로젝트 선택이 필수**입니다. 프로젝트 없이 간단히 쓰려면 **간단 대화**(`/simple`) 메뉴를 사용하세요.

**Q. 지침과 가이드라인 차이가 뭔가요?**  
A. **지침(instructions)**은 한 덩어리 텍스트로, 프로젝트 내 모든 대화에 공통 적용됩니다. **가이드라인(initialGuidelines)**은 목록 형태로, 노트북 LLM 등에서 참고하는 가이드라인입니다. 둘 다 프로젝트 편집 모달에서 설정합니다.

**Q. 노트북 LLM에서 프로젝트가 반영이 안 돼요.**  
A. **CORBU.AI** 메뉴에서 먼저 **프로젝트를 선택**한 뒤 **노트북 LLM** 메뉴로 이동해야 합니다. 같은 브라우저 탭에서 순서만 지키면 됩니다.

**Q. 목소리 생성(TTS)이 안 돼요.**  
A. (1) **고급 기능** → **목소리 생성** 탭을 선택했는지 확인. (2) URL 사용 시 URL이 유효한지, 프로젝트 보이스 사용 시 해당 프로젝트에 보이스 소스가 등록되어 있는지 확인. (3) 브라우저 F12 → Network에서 `/api/tts/` 요청 실패 여부 확인.

**Q. 프로젝트/대화를 삭제했는데 복구할 수 있나요?**  
A. **복구 불가**입니다. 삭제 전에 필요한 내용은 따로 복사해 두세요.

**Q. 여러 프로젝트를 동시에 쓸 수 있나요?**  
A. **한 번에 선택되는 프로젝트는 하나**입니다. 다른 프로젝트를 쓰려면 사이드바에서 해당 프로젝트를 클릭해 전환하면 됩니다.

**Q. 파일 업로드가 느리거나 실패해요.**  
A. 파일 크기·형식 제한을 확인하고, 네트워크가 안정적인지 확인하세요. F12 → Network에서 업로드 요청(`/api/projects/.../files`) 실패·타임아웃 여부를 확인할 수 있습니다.

**Q. 포트 3000 / 5002가 이미 사용 중이라고 나와요.**  
A. [8.7 포트가 이미 사용 중일 때](#87-포트가-이미-사용-중일-때)를 참고해, 다른 포트로 실행하거나 해당 포트를 쓰는 프로세스를 확인·종료하세요.

**Q. 대화 제목은 어떻게 정해지나요?**  
A. **첫 메시지 전송 후** 시스템이 자동으로 생성합니다. 사용자가 직접 바꿀 수 있는 기능이 있다면 해당 메뉴(대화 제목 편집 등)를 사용하세요.

**Q. 프로젝트 파일은 몇 개까지 올릴 수 있나요?**  
A. **최대 개수·용량**은 백엔드 설정에 따라 다릅니다. 업로드 실패 시 [§4.7 입력·저장 제한](#47-입력저장-제한-참고)과 [§8.3 대화/프로젝트가 저장되지 않음](#83-대화프로젝트가-저장되지-않음)을 참고하고, 파일 크기·형식을 줄이거나 나눠 올려 보세요.

**Q. 새로고침(F5)하면 대화·프로젝트가 사라지나요?**  
A. 대화·프로젝트는 **브라우저 로컬 스토리지**에 저장되므로, 일반적으로 새로고침해도 **유지**됩니다. 다만 시크릿/비공개 모드, 로컬 스토리지 삭제, 다른 브라우저·기기에서는 보이지 않습니다. [§1.5 지원 환경](#15-지원-환경-참고)·[§4.7 입력·저장 제한](#47-입력저장-제한-참고) 참고.

**Q. 음성 인식·이미지 분석은 어디서 쓰나요?**  
A. **고급 기능** 메뉴 클릭 후 메인 영역에서 **음성 인식**, **이미지 분석**, **예측** 등 탭/카드를 클릭하세요. 각 기능 화면의 입력 필드·버튼 안내에 따라 사용합니다. [§6.3 고급 기능 탭 구성](#63-고급-기능-탭-구성) 참고.

---

## 11. 참고 링크

| 문서 | 용도 |
|------|------|
| [메뉴얼 빠른 참조](./docs/guides/MANUAL_QUICK_REFERENCE.md) | 한 페이지 요약(화면 구조·핵심 3단계·버튼 위치·접속 주소·체크리스트) |
| [빠른 시작](./QUICK_START.md) | 5분 안에 실행·접속·첫 대화 |
| [실행 가이드](./RUN_GUIDE.md) | 실행·배포 방법 상세 |
| [LLM 설정 가이드](./backend/LLM_SETUP_GUIDE.md) | LLM 환경 변수·모델 설정(Ollama·OpenAI·Anthropic) |
| [TTS·스크립트 스타일 가이드](./docs/guides/TTS_AND_SCRIPT_STYLE_GUIDE.md) | 목소리 생성(TTS) 스타일·API·검증 |
| [프로젝트·노트북 LLM 사용자 가이드](./docs/PROJECT_NOTEBOOK_LLM_USER_GUIDE.md) | 프로젝트·노트북 LLM 사용법 상세 |
| [API 문서](http://localhost:5002/api/docs) | 백엔드 API (실행 후 접속) |

---

### 이 문서에서 찾기 (키워드 → 섹션)

| 찾는 내용 | 섹션 |
|-----------|------|
| 이 문서 읽는 방법, 처음 사용·이미 사용 중·문제 발생 시 | 문서 상단 "이 문서 읽는 방법" |
| 화면 레이아웃, 사이드바, 메뉴, 대화 목록 순서, 모바일, 햄버거, 지원 환경, 브라우저, 로컬 저장 | §1 화면 구성 |
| 실행 방법, 종료(Ctrl+C), 개별 실행, 접속 주소, 다른 기기 접속, LLM 설정 | §2 시작하기 |
| CORBU.AI·간단 대화·고급 기능·전체 기능 맵·노트북 LLM 역할, 메뉴 URL·직접 접속 | §3 메뉴별 기능 |
| 프로젝트 생성·선택·편집·삭제·공유·링크·권한·지침·파일·편집 모달 필드·입력 제한 | §4 프로젝트 관리 |
| 새 대화·기존 대화·전송·줄바꿈·파일 첨부·드래그 앤 드롭·마크다운·복사·스트리밍·대화 삭제·시나리오 | §5 대화 |
| 목소리 생성(TTS)·노트북 LLM·보이스 소스·딥러닝·고급 기능 탭·음성 인식·이미지 분석·탭 전환 | §6 고급 기능 |
| 단축키·Tab·포커스 이동·확인 포인트·자주 보이는 메시지 | §7 키보드 단축키 |
| 문제 해결·포트 충돌 | §8 문제 해결 |
| 프로젝트·대화·지침·가이드라인·프로젝트 파일·보이스 소스 등 용어 | §9 용어 정리 |
| FAQ, 대화 제목, 파일 개수·용량, 새로고침·로컬 저장 | §10 자주 묻는 질문 |
| 예상 결과, 정상 동작 시 보이는 것 | §5.6 시나리오, §7.1 확인 포인트 |
| 무엇을 할 때 어디를 누르나요 | 목차 아래 표 |
| 문서 범위, 갱신 | 문서 끝 "문서 정보" |
| 참고 링크, 관련 문서, RUN_GUIDE, LLM 설정, TTS 가이드 | §11 참고 링크 |

---

### 메뉴얼 문서 구성

| 문서 | 용도 |
|------|------|
| **본 문서(USAGE_GUIDE.md)** | 화면 구성, 시작하기, 메뉴별 기능, 프로젝트·대화·고급 기능 상세, 시나리오별 예상 결과, 단축키, 문제 해결, 용어, FAQ, 참고 링크. |
| **[메뉴얼 빠른 참조](./docs/guides/MANUAL_QUICK_REFERENCE.md)** | 한 페이지 요약(화면 구조·주의·팁·핵심 3단계·버튼 위치·접속 주소·실행 전 체크리스트·파일 형식). |
| **[빠른 시작](./QUICK_START.md)** | 5분 안에 의존성 설치·실행·접속·첫 대화. 단계별 정상 시 확인 포함. |
| **[guides 사용 가이드](./docs/guides/USAGE_GUIDE.md)** | CORBU.AI 기준 짧은 가이드(프로젝트·파일·지침·대화 테스트·체크리스트·예상 결과). |
| **[TTS·샘플 스타일 가이드](./docs/guides/TTS_AND_SCRIPT_STYLE_GUIDE.md)** | 목소리 생성(TTS) 속도·구간별 조절·샘플 대본 스타일·감정 프리셋. |
| **[노트북 LLM 사용 가이드](./docs/PROJECT_NOTEBOOK_LLM_USER_GUIDE.md)** | 노트북 LLM·프로젝트 연동 상세. |

---

### 문서 정보

- **범위**: CORBU.AI 웹 앱의 화면·메뉴·프로젝트·대화·고급 기능(TTS·노트북 LLM) 사용법. API·배포·개발 환경 상세는 RUN_GUIDE, LLM_SETUP_GUIDE 등 별도 문서 참고.
- **갱신**: 기능 추가·변경 시 본 메뉴얼이 함께 갱신됩니다. 최신 버전은 저장소(README·docs)를 참고하세요.

---

**CORBU.AI와 함께 효율적으로 작업하세요.**
