# Agent / AI 개발 가이드

이 프로젝트에서 AI 어시스턴트(에이전트)가 코드를 수정하거나 제안할 때 참고할 기준입니다.

## 우선 참고 문서

- **[docs/setup/PYTHON_VENV.md](./docs/setup/PYTHON_VENV.md)** — 백엔드 Python·가상환경 우선순위 (`lib-activate-backend-venv.sh`, `lib-backend-python.sh`, `restart:backend`)
- **[docs/DEVELOPMENT_SCOPE_MASTER.md](./docs/DEVELOPMENT_SCOPE_MASTER.md)** — 개발 범위 통합 (비전·아키텍처·단계·품질 기준)
- **[docs/COMPONENT_ARCHITECTURE.md](./docs/COMPONENT_ARCHITECTURE.md)** — 라우트→뷰·메시지 UI·프로젝트 관리(활성/비활성)·개발 연속성 체크리스트
- **[docs/DEVELOPMENT_CONTINUITY.md](./docs/DEVELOPMENT_CONTINUITY.md)** — 경로·컴포넌트 매핑·체크리스트·관련 문서 일람 (기능 추가 시 진입점)
- **[DEVELOPMENT.md](./DEVELOPMENT.md)** — 프로젝트 구조, 일상 개발 흐름, 실행·테스트 명령
- **[docs/COMPONENT_ARCHITECTURE.md](./docs/COMPONENT_ARCHITECTURE.md)** — 컴포넌트·뷰·서비스 매핑, 메시지 UI 스택, 프로젝트 관리(활성/비활성)
- **[docs/BACKLOG.md](./docs/BACKLOG.md)** — 진행 중·단기·중기 작업 목록 (새 작업 추가·완료 체크는 여기서)
- **[docs/COMPLETION_CHECKLIST.md](./docs/COMPLETION_CHECKLIST.md)** — 완성도·마무리 검증 순서 (§6), `npm run verify:completion`
- **[docs/PUSH_BLOCK_HANDOFF.md](./docs/PUSH_BLOCK_HANDOFF.md)** — 원격 `git push` 막힘 시 로컬 이관·점검(`npm run maintain:push-block` / `make maintain-push-block`)·사이드바·**컴포저**·**관계도** 회귀(`test:sidebar-context`·`verify:composer-pipeline`·`verify:conversation-graph:unit`; 수동 §14.5·§14.7 [CHAT_UI_TEST_SCENARIOS](./docs/guides/CHAT_UI_TEST_SCENARIOS.md))
- **[TESTING_GUIDE.md](./TESTING_GUIDE.md)** — 테스트 구조·검증 명령·`test:sidebar-context`·`verify:composer-pipeline`(§14.7)·`verify:conversation-graph:unit`·활성 md 허브 `check:doc-verification-hub`·백엔드 pytest 목록
- **[docs/guides/TTS_AND_SCRIPT_STYLE_GUIDE.md](./docs/guides/TTS_AND_SCRIPT_STYLE_GUIDE.md)** — 목소리 생성(TTS) 속도·샘플 스타일·감정 프롬프트·API·검증 (`npm run test:tts:all`)

## 병렬·2배 속도로 밀어붙일 때 (작업 분할)

한 세션에서 **두 갈래 이상** 동시에 진행해 처리량을 늘립니다.

| 갈래 A (프론트) | 갈래 B (백엔드/계약) |
|-----------------|----------------------|
| `ChatGPTInterface` / `buildUnifiedChatContext` / `chatInputUtils`·`streamingClient` | `question_answer_pipeline` · `unified_chat_api.py` |
| `GensparkPipelineExtrasPanel`·스트리밍 `metadata` 파싱 | OpenAPI `docs/api/openapi-unified-chat.yaml` `ChatContext`·`StreamMetadata` |

- **한 번에 끝낼 체크**: 타입·린트 해당 파일만 (`read_lints`). 파이프라인 관련 **백+프론 동시**: **`npm run test:dev:dual-pipeline`** — 백엔드 `test:backend:pipeline-smoke`(`scripts/run-backend-pipeline-smoke.sh`, `lib-backend-python.sh`로 `venv`/`.venv` 중 `pytest` 가능 Python) + 프론트 `test:frontend:chat-pipeline` (메타 파싱·SSE·`buildUnifiedChatContext`·`GensparkPipelineExtrasPanel`). **튜닝 API·노트북 context**: `npm run test:backend:pipeline-tuning`.
- **환경 한 줄로 기능 켜기**: [DEEPSEEK_SETUP §2.3](docs/DEEPSEEK_SETUP.md) — `REACT_APP_PIPELINE_VERIFIER_REWRITE`, `REACT_APP_SIMPLE_CHAT_USE_PIPELINE`, 서버 `PIPELINE_VERIFIER_REWRITE`.
- **아키텍처 순서**: [GENSPARK_REPO_IMPLEMENTATION_ORDER.md](docs/architecture/GENSPARK_REPO_IMPLEMENTATION_ORDER.md) 체크리스트와 병행하면 문서·코드가 같이 맞춰짐.

## 규칙 요약

1. **구조**: 프론트는 `src/`, 백엔드는 `backend/`. API 라우터는 `backend/api/`, 테스트는 `backend/tests/`.
2. **테스트**: 백엔드 API 추가/수정 시 `backend/tests/`에 해당 테스트 추가. 커밋 전 `npm run dev:check` 권장.
3. **작업 추적**: 작업 완료 시 `docs/BACKLOG.md`에서 해당 항목 체크. 새 태스크는 적절한 우선순위 구간에 추가.
4. **Cursor 규칙**: `.cursor/rules/` 에 프로젝트 구조·백엔드 테스트 규칙 등이 정의되어 있음. `session-continuity.mdc` — 세션 종료·팅김 방지(작업 단위 분할·재개 절차). **무거운 명령(verify:completion, test:backend 등)은 실행하지 말고, 사용자에게 외부 터미널 실행을 안내.** 자주 종료될 때: [docs/CRASH_PREVENTION.md](docs/CRASH_PREVENTION.md)
5. **문서·긴 글·URL·요구 기반 편집**: 원문을 바꾸거나 스펙에 맞출 때는 `.cursor/skills/spec-driven-document-edit/SKILL.md`(요구 추출 → 계획 → 적용 → 검증, 링크는 본문 확보 후). 항상 로드: `spec-driven-document-edit-always.mdc`.

**검증 명령**: `npm run verify:completion` (마무리 검증: 타입+린트+P4 `test:p4:services`) · `npm run verify:composer-pipeline` (다중 요청·첨부·순차·다단계 Jest; `make test-composer-pipeline`) · `npm run verify:conversation-graph:unit` (관계도 Jest·백엔드; `make test-conversation-graph-unit`) · `npm run check:doc-verification-hub` (활성 md 허브 단락; 가벼움, `DOC_HUB_STRICT=1`은 CI 동일) · `npm run test:views` (뷰·라우트 22 suites, 137 tests) · `npm run p2:check` (P2 1·2단계: verify+test:views, 3~5 PERFORMANCE.md §2.6) · `npm run dev:check` (백엔드 144 + 타입 + lint) · `npm run test:tts:all` (TTS 252+11) · `npm run test:p4:services` (9 suites, 158 tests) · `npm run test:views:services` 또는 `npm test -- --testPathPattern=ViewService` (도구 뷰 서비스 10 suites, 45 tests).

**대화·답변**: API 호출 시 `context.conversation_history`(최근 20턴), `project_id`(노트북 LLM), `request_id`·`diversity`·`temperature`(같은 질문 n번 다른 답변), `quality`(basic|enhanced|ultimate) 전달. **통합 파이프라인(Q→A·Genspark·프로젝트 없음)**: `mergeApiChatContextPayload`(파이프라인 블록이 없어도 `buildFeatureContextFromMessage`로 다중 요청·플래그 보강, `multi_request_mode` 시 `multi_request_adaptation_instruction` 포함) / `buildUnifiedApiChatRequestBody` 경로 — 스트리밍(`streamingClient.streamChatMessage`)·`apiClient.sendChatMessage`·`integratedSystemAPI.sendMessage`(context 내 턴 자동 인식)·`unifiedMessageService`·`integratedMessageService`(대화형 분기 `POST /api/chat`, 본문 `message`+병합 `context`; 자동 라우팅 시 단독「메시지」는 대화형으로 두고 가이드·guidance·「메시지 가이드」·문구 추천 등만 `/api/guidance`)·`FileAnalysisChatSystem`/`UltimateChatGPTInterface`·`ChatGPTInterface` 비스트리밍(`buildChatGptNonStreamPostPayload`)·루트 `App.js`·`views/SimpleChatView`(`/api/unified/chat` 및 `/api/chat` 폴백)의 POST도 동일 병합. `context` 안의 `conversation_history`만 있어도 파이프라인 턴으로 인식. **한 메시지에 여러 질문·요구**(번호·불릿·접속사 줄): `chatInputUtils.parseMultiAskItems`·`buildFeatureContextFromMessage`가 항목별 기능 플래그를 합집합하고 `multi_request_items`·`multi_request_mode`·`multi_request_adaptation_instruction`을 `buildUnifiedChatContext`가 넣음 — 매 요청 내용에 맞게 답 구조·기능을 바꿈. **컴포저 UI**: 질문·요구·요청 칩·입력 미리보기(`chat-composer-input-hint`)·5단계(`composer-genspark-generation-status`)·체크리스트(`composer-multi-request-checklist`). **순차 API(옵트인)**: `REACT_APP_COMPOSER_SEQUENTIAL_MULTI_REQUEST`·`..._STREAM`. **다단계(옵트인)**: `REACT_APP_COMPOSER_MULTI_STEP_MULTI_REQUEST` — `runComposerMultiStepMultiRequest`(순차보다 우선순위 낮음). `composerSequentialSend`·전송·재생성·편집 공통. 검증: `npm run verify:composer-pipeline` · [CHAT_UI_TEST_SCENARIOS §14.7](./docs/guides/CHAT_UI_TEST_SCENARIOS.md). 백엔드: `unified_chat_api._compose_multi_request_instruction` → context `_multi_request_instruction` → `llm_service` 프롬프트 prefix·`intelligent_response_engine` considerations. 스트리밍(`/api/chat/stream`)도 `_ensure_original_message_and_adapt_defaults`로 원문·adapt 기본값 동일 적용; `multi_request_mode`일 때는 `_split_multiple_questions` 생략해 통합 답변과 충돌 방지. `generate_chat_response` 진입 직후·`question_answer_pipeline` `_normalize_and_context_load`에서 `_compose_multi_request_instruction`으로 `_multi_request_instruction`을 채워 Q→A 조기 반환·직접 `run_pipeline` 호출에도 동일 반영. `REACT_APP_MODERN_CHAT_UNIFIED_CONTEXT=0`이면 Modern Chat·Integrated Master 등 보조 UI가 파이프라인 블록을 보내지 않음. **Genspark 외부 에이전트 폼 반영**: `REACT_APP_GENSPARK_REFERENCE_AGENT_INSTRUCTIONS`, `REACT_APP_GENSPARK_REFERENCE_AGENT_PROFILE=0`, 서버 `GENSPARK_REFERENCE_AGENT_INSTRUCTIONS` — 상세 [docs/architecture/GENSPARK_EXTERNAL_AGENT_FORM_MAPPING.md](docs/architecture/GENSPARK_EXTERNAL_AGENT_FORM_MAPPING.md). **Context 계약·다중 요청 키 정리**: [docs/guides/CHAT_CONTEXT_CONTRACT.md](docs/guides/CHAT_CONTEXT_CONTRACT.md). 에러·응답: getErrorMessage 401/404/429, extractResponseContent 안전 추출. 흐름·품질 검증: [docs/guides/CHAT_ANSWER_FLOW_VERIFICATION.md](docs/guides/CHAT_ANSWER_FLOW_VERIFICATION.md), [docs/guides/ANSWER_QUALITY_AND_SEARCH.md](docs/guides/ANSWER_QUALITY_AND_SEARCH.md). **딥시크(DeepSeek)**: provider가 deepseek/deepseek-local이면 대화에서 LLM 우선 시도. 동작 안 할 때: [docs/DEEPSEEK_SETUP.md §4.1](docs/DEEPSEEK_SETUP.md#41-딥시크가-동작하지-않을-때-체크리스트). 한 흐름: [docs/DEEPSEEK_INSTALL_RUN_DEVELOP_LEARN.md](docs/DEEPSEEK_INSTALL_RUN_DEVELOP_LEARN.md). 상세: BACKLOG 27·28·29차, DEVELOPMENT.md §2.3 아래.

**노트북 LLM·분야별 지식·글쓰기 스타일·딥러닝**: DOMAIN_KNOWLEDGE_BASE keyPoints, buildIntelligentContext, getStyleInstruction, buildDLPromptEnhancement(sentiment). NOTEBOOKLM_FEATURE_AND_KNOWLEDGE_CHECKLIST §2.4·§2.5, BACKLOG 33·34·35차.

**프로젝트 파일 업로드**: POST /api/projects/{id}/files 백엔드·프론트 연동. projectService.uploadProjectFile, ProjectEditModal 파일 추가(API·로컬 폴백·업로드 중 로딩). COMPLETION_CHECKLIST §2 "프로젝트 파일 업로드" 행, BACKLOG 85~91차. **프로젝트 관리 UI**: 활성 — ProjectEditModal·ProjectHub(ProjectsPage). 비활성 — ProjectCreationModal·ProjectList(backup). COMPONENT_ARCHITECTURE §3 참고.

**라우트·메뉴**: config/routes 3분할 — 대화(/), 프로젝트(/projects), 프로젝트대화(/projects/:id). 목소리 생성(/voice-generation) 별도 메뉴. 질문·요구 입력 시 available_capabilities·buildFeatureContextFromMessage로 기능 플래그 전달. BACKLOG 102~117차.

**확장 뷰(도구 메뉴)**: 설정·분석·도움말·템플릿·검색·연동·팀·학습·구독·워크스페이스·자동화·커뮤니티 12개. src/views/ 섹션·카드·aria-labelledby 통일. 검증: `npm run test:views` (뷰+라우트), `npm run test:views:services` 또는 `npm test -- --testPathPattern=ViewService` (도구 뷰 API 서비스 45 tests), E2E example.spec(뷰+h2). src/views/README.md §확장 뷰 검증, DEVELOPMENT_SCOPE_MASTER §2.2.

**대화 기능 통합**: context.available_capabilities, request_capability_help(기능 안내 요청), enable_web_research, investigative_mode 등. 백엔드 _run_pre_generation_pipeline에서 처리. ⌘? 단축키 도움말(KeyboardShortcutsHelp).

**대화 전송(UI)**: `onClick={handler}`는 **SyntheticEvent**를 첫 인자로 넘김 — `handler(optionalText?: string)` 시그니처면 `onClick={() => void handler()}`로 감쌀 것. 입력 문자열·trim은 `chatInputUtils.coerceTrimmedString(primary, fallback)`로 통일(`chatInputUtils` 내부 파싱·응답 추출·메타 정리도 동일; 원시 `.trim()`은 이 함수 본문만). **끝 공백만** 제거할 때는 `coerceTrimmedEnd`(`trimEnd` + 비문자 방어). 예: `ChatGPTInterface.sendMessage`·메시지 편집 저장·대화 제목 저장·대화 내 검색·사이드바 통합 검색·입력창 `getCurrentInputValue`/폼 제출·`buildChatContext` 지침·가져오기(JSON/MD/HTML)·문체 학습·TTS 정리·스트리밍 응답 검증, `ModernChatInterface`, `IntegratedMasterInterface`, `UltimateChatGPTInterface`, `ChatGPT5CompleteInterface`, `ChatGPTStyleInterface`, `IntegratedAIChat`, `Chat/ChatInterface`, `FileAnalysisChatSystem`, `AdvancedFeaturesPanel` 메시지 품질 예측·목소리 생성(TTS) 입력·URL·프로젝트 ID, `AdvancedAIEngine` 처리 입력, `views/SimpleChatView`, `App.js` 루트 대화·퀵 메시지, `useChatManagement.handleSaveEdit`, `Chat/MessageEditForm`, `SessionManager`, `Layout/Sidebar` 프로젝트 생성, `WebResearchModal`/`DeepResearchModal` 검색 쿼리·소스 URL, `News/NewsSearch`, `NotebookLLM` 프롬프트·소스·정관·복사, `ConversationGraphView` 붙여넣기 업로드, `AICodeGenerator`/`AIDesignSystem` 생성 프롬프트, `ProjectCreationModal` 이름·태그·지침, `ProjectCreateModal`/`ProjectEditDialog`, `ProjectManagement/ProjectEditModal` 저장·태그·웹소스·가이드라인 일괄/히스토리/프리셋·URL 정규화, `NotebookLLM` 프롬프트 히스토리·도메인 템플릿 치환·스튜디오 TTS 정리, `ModernChatInterface` 타이핑/스마트 제안, `UltimateChatGPTInterface` 복원·렌더, `FileAnalysisChatSystem` 노트 지식 플래그, `MarketingContent`/`PersuasionContent`/`CreativeWriting`, `AdvancedAIFeatures` 감정·요약·키워드, `MessageModifyRequestDialog`, `WritingQualityPanel`, `MessageEditor`, `Search/AdvancedSearch`, `AdvancedSearchPanel`, `Collaboration/RealTimeCollaboration` 초대 이메일, `EnhancedUserExperience`/`AIManagement` 피드백, `Guidelines/GuidelinesManager`, `projectService`(URL·YouTube), `notebookLLMDeepLearningIntegration`, `utils/guidelinePolicyPack`, `generationPromptBuilder`, `notebookLLMService` 정관, `AppUnified`/`ProjectHub` 검색, `WritingAssistant`, `AIEngineMonitor`, `genspark/GensparkPipelineExtrasPanel`, `qwenTtsService`·`intentApi`, `chatService` 텍스트/키워드, `textToSpeechService`·`speechRecognitionService`, `questionRequirementExpander`, `associationBylawsService`, `advancedSecurityService` 정책명, `realTimeKnowledgeSystem`, `predictiveAnalysisEngine`, `webCommentAnalysisService`, `intelligentResponseSystem`, `knowledgeService` 전처리, `clientFileProcessor` 통계·액션·개념·문장 분할·상세 콘텐츠, `utils/errorMessages` URL, `rehypeHighlightSearch`, `advancedSearchParser`, `guidelineQuality`, `knowledgeIntegrationService`, `commentAndRebuttalSystem`, `advancedResponseGenerator`, `utils/kakaoParser`, `SearchPanel`, `WritingEditor`, `ProjectTemplateSelector`, `Chat/VoiceRecognition`, `WritingAISuggestions`, `WritingStatisticsDashboard`, `Chat/ChatMessage`, `AdvancedAnalyticsDashboard`, `WritingStyleSelector`, `conversationalAnalysisService` 주제·스타일 추출, `advancedLogicAnalysisEngine` 인과 체인, `platformSpecificWritingEngine` 문장 정리, `socialMediaInteractionEngine` 논점, `Analytics/ConversationSummary` 요약 텍스트, `qwenTtsService` 프로젝트 TTS 지시, `chatDataService` 카카오/샘플 파싱, `intelligentKnowledgeProcessor` 섹션·응답 줄, `mediaAnalysisService` 액션·참고, `projectKnowledgeService` 스니펫·컨텍스트, `advancedFileProcessingService` CSV·문단, `multimodalAIService` 테이블·코드·요약·함수 인자, `conversationStyleAnalyzer` 메시지 본문, `notebookLLMService` 판례 토큰, `aiService` 추천 줄, `intelligentResponseEngine` 질문 전처리·요약·명확성, `chatgpt5LevelService` 문장 구조, `advancedQuestionAnalyzer` 분할·직접 질문, `messageHistoryService`/`searchHistoryService` 검색어, `conversationMemoryService` 질문 정규화, `realEstateService` 지역명·요약·예측·대시보드 템플릿, `koreanUnderstandingLayer` 표면 정규화, `streamingClient` JSON 폴백·SSE payload, `localLLMService` 스트리밍 줄, `aiQualityEnhancementService` 구조 개선, `advancedNLPService` 복잡도·논리 구조, `securityReportService` HTML, `contextualUnderstandingService` 줄 구조, `advancedFileProcessingService` 요약, `aiResponseQualityService` 명확성, `advancedTextAnalysisService` 기술 분석, `newsService` 요약, `webSearchIntegrationService` 답변 문장, `adaptiveWritingEngine` 축소·확장·메타, `advancedNLPEngine` 복잡도, `automationWorkflowEngine` 명확성, `advancedAIFunctions` 품질 평가, `advancedConversationProcessor` 복합 입력 분할, `searchHistoryService` 저장, `styleAnalysisEngine`, `creativeWritingAIEngine`, `knowledgeIntegrationService` 사실 문장, `mediaAnalysisService` 음성·복잡도, `platformSpecificWritingEngine` 문장 필터, `advancedLogicAnalysisEngine` `splitIntoSentences`, `multiLayerStyleAnalysisSystem`, `realTimeWritingCoachingSystem`, `styleCloneEngine`, `enhancedResponseProcessor`, `advancedWritingEngine` 단어 수, `writingQualityAnalyzer`, `answerCritic`, `contextualResponseEnhancer`, `professionalWritingEngine`, `notebookLLMStreamingService`). `ChatGPTInterface`는 렌더 공통으로 `inputTrimmed`(`useMemo`+`coerceTrimmedString`) 사용 권장. 퀵 액션 등으로 `setState` 직후 바로 전송할 때는 **`void handler(같은문자열)`**로 override 전달(상태 비동기로 빈 입력 전송 방지). `type="submit"` 전송 버튼은 폼 `onSubmit`만 쓰고 **중복 `onClick={send}` 제거**(이중 전송 방지). 상세: [TESTING_GUIDE.md](./TESTING_GUIDE.md).

**보조 트리 `frontend/src`**: CRA 빌드는 루트 `src/`만 사용합니다. `chatInputUtils`를 메인과 맞출 때는 **`npm run sync:frontend-chat-input-utils`**(루트 `src/utils/chatInputUtils.ts` → `frontend/src/utils/chatInputUtils.ts` 복사). 대량 치환·import 삽입은 `scripts/frontend_coerce_trim.py` 참고(import는 **각 `import … from` 블록이 끝난 뒤**에 넣도록 스크립트 처리).

이 파일과 DEVELOPMENT.md, BACKLOG.md를 함께 사용하면 체계적으로 개발을 이어갈 수 있습니다.

## 개발자 검증

저장소 루트 검증 허브: [TESTING_GUIDE.md](TESTING_GUIDE.md) — `npm run test:routes` · `npm run test:views`(확장 뷰·라우트) · (권장) `npm run test:sidebar-context`(§14.5) · (권장) `npm run verify:composer-pipeline`(§14.7) · (권장) `npm run verify:conversation-graph:unit` · (선택) `npm run check:doc-verification-hub` · 마무리 `npm run verify:completion` — [COMPLETION_CHECKLIST.md](docs/COMPLETION_CHECKLIST.md) · 배포 직전 [FINAL_CHECKLIST.md](docs/FINAL_CHECKLIST.md)(`npm run verify:final`) · 원격 `git push` 막힘 [PUSH_BLOCK_HANDOFF.md](docs/PUSH_BLOCK_HANDOFF.md)(`npm run maintain:push-block`).

