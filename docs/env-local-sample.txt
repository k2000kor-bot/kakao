# 로컬 웹 접속용 환경 변수 예시
# 사용: 이 내용을 복사해 프로젝트 루트( package.json 있는 폴더 )에 .env.local 파일로 저장
#
# Create React App: PORT, REACT_APP_* 만 클라이언트에 노출됩니다.

# 개발 서버 포트 (기본 3000)
# PORT=3000

# API를 프록시 대신 직접 지정 (비우면 package.json 의 proxy 로 /api → localhost:5002)
# REACT_APP_API_URL=http://localhost:5002
